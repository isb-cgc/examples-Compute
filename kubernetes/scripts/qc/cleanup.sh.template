if [ -f share/{filename}.success ]; then 
	gsutil -o Credentials:gs_oauth2_refresh_token=$(cat /data-access/refresh-token) -o Oauth2:oauth2_refresh_retries=50 cp share/{basename}* {destination}
else echo 'Success indicator file not found'; fi
