if [ ! -f share/{filename} && ! -f share/{filename}.success ]; then 
	gsutil -o Credentials:gs_oauth2_refresh_token=$(cat /data-access/refresh-token) -o Oauth2:oauth2_refresh_retries=50 cp {url} {url}.bai share
else echo 'File {filename} already staged -- skipping'; fi
